<h1>FORM</h1>
From
  <select name="departure_airport_id">
  <% @airports.each do |airport|%>
<option value=<%= airport.id%>><%=airport.name%></option>
<% end %>

    </select>

To     <select name="destination_airport_id">
  <% @airports.each do |airport|%>
<option value=<%= airport.id%>><%=airport.name%></option>
<% end %>

    </select>

Date
  <select name="flight_date">
<option>month</option>
<option>2mgosk</option>
<option>gnun3</option>

    </select>
Number of Passengers
 <select name="passengers_number">
<option>1</option>
<option>2</option>
<option>3</option>
    </select>



<h1>Flights</h1>

<h2>
<%= link_to "Airports", "airports#index"%>
</h2>
<table>

  <thead>
    <tr>
           <th>Airline</th>

      <th>Departure airport</th>
      <th>Destination airport</th>
      <th>Date</th>
      <th>Departs at</th>
            <th>Arrives at</th>

      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
  <td><br></td>

    <% @flights.each do |flight| %>
      <tr>
        <td><%= flight.airline %></td>

        <td><%= flight.departure_airport.name %></td>
        <td><%= flight.destination_airport.name %></td>
        <td><%= flight.flight_date %></td>
        <td><%= flight.departure_time%></td>
        <td><%= flight.arrival_time%></td>


        <% end %>
      </tr>
  </tbody>
</table>